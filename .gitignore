# Secrets — never commit credentials
.env
.env.*
!.env.example
.aws/

# Scraped/processed data — S3 is the source of truth, nothing local in git
tools/scrapers/entsoe_bg/
tools/scrapers/weather_bg/
tools/scrapers/weather_forecasts/
tools/**/*.csv
tools/**/_summary*.json

# Model figure outputs + HTML report — uploaded to S3 data/results/, not committed
model/results/

# Local storage backend mirror (STORAGE_BACKEND=local / --local) — not committed
local_store/

# macOS
.DS_Store

# Python
__pycache__/
*.py[cod]
.ipynb_checkpoints/
.venv/
venv/

# IDE
.idea/
.vscode/
