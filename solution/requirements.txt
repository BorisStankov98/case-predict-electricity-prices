# Minimal runtime for the forecasting pipeline.
# Tested with the Anaconda Python 3.13 stack.
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3      # HistGradientBoostingRegressor (native NaN handling)
matplotlib>=3.6
# Optional, not required (BG holidays are hard-coded for the data span):
# holidays>=0.40
